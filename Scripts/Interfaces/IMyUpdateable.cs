
namespace ChessOut.Interfaces
{
    //An interface for every class that needs to be called during update
    public interface IMyUpdateable
    {
        public void Update(GameTime gameTime);
    }
}
